import React, { Component } from "react";
import { View, Text, StyleSheet } from 'react-native';

export default class AssignTable extends Component{
  render(){
    return(
      <View>
        <Text>Assign Table Screen</Text>
      </View>
    );
  }
}
const styles = StyleSheet.create({

});